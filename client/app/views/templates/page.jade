#content
    #errors
        p Erreur
        p#errorText
        span#closeError(class="on-error") ok

    #sidebar
        ul#servicesMenu

        span(class="exitButton")
            a(href="#logout")
                i(class="fa fa-sign-out")
                span Réinitialiser l'application

    iframe#app(src="#{url}")