#content
    #errors
        p Erreur
        p#errorText
        span#closeError(class="on-error") ok

    #sidebar
        ul#servicesMenu

        span(class="exitButton")
            a(href="#logout")
                i(class="fa fa-sign-out")
                span Déconnexion

    iframe(src="#{url}")